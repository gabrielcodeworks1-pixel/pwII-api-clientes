generetor client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "mysql"
    url = env("DB_CONNECTION")
}

model Client {
    id Int @id default(autoincrement())
    nome String 
    telefone String?
    endereco String?

    @@map("clientes")
}